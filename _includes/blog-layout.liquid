<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Meeshkan Blog</title>
    </head>
    <body>
        <h1>Meeshkan Blog</h1>
        <h2>{{ title }}</h2>

        {% if author %}
          By <a href="{{ authorLink }}">{{ author }}</a>
        {% endif %}
        
        {{ content }}
    </body>
</html>